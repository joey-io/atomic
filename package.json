{
  "name": "atomic",
  "version": "0.0.1",
  "description": "Atomic — a data substrate where schema, data, identity, and the UI surface are all atoms.",
  "type": "module",
  "scripts": {
    "start": "node kernel.mjs",
    "test": "node test.mjs",
    "audit": "node kernel.mjs --audit",
    "seed": "node seed-a.mjs && node seed-b.mjs && node seed-c.mjs && node seed-d.mjs"
  },
  "license": "MIT"
}
