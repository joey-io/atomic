{
  "name": "atomic",
  "version": "0.0.1",
  "description": "Atomic — a data substrate where schema, data, identity, and the UI surface are all atoms.",
  "type": "module",
  "scripts": {
    "start": "node atomic.mjs",
    "test": "node test.mjs",
    "audit": "node atomic.mjs --audit",
    "seed": "node seeds/seed-a.mjs && node seeds/seed-b.mjs && node seeds/seed-c.mjs && node seeds/seed-d.mjs"
  },
  "engines": {
    "node": ">=22.5"
  },
  "license": "MIT"
}
