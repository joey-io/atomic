{
  "name": "atomic",
  "version": "0.1.0",
  "description": "An evidence-backed data substrate and parser runtime powered by installable model packs.",
  "type": "module",
  "main": "./src/index.mjs",
  "exports": {
    ".": "./src/index.mjs",
    "./core-models": "./src/models/core.mjs",
    "./kernel": "./atomic.mjs"
  },
  "files": [
    "src/",
    "atomic.mjs",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "start": "node atomic.mjs",
    "test": "node test.mjs && node test-parser.mjs",
    "test:parser": "node test-parser.mjs",
    "check": "node atomic.mjs --check",
    "audit": "node atomic.mjs --audit",
    "seed": "node seeds/seed-a.mjs && node seeds/seed-b.mjs && node seeds/seed-c.mjs && node seeds/seed-d.mjs"
  },
  "engines": {
    "node": ">=22.5"
  },
  "keywords": [
    "knowledge-graph",
    "semantic-models",
    "entity-extraction",
    "parser",
    "ontology"
  ],
  "license": "MIT",
  "comment": "Zero required dependencies. `pg` is OPTIONAL — loaded only when the kernel uses the Postgres driver.",
  "optionalDependencies": {
    "pg": "^8.21.0"
  }
}
